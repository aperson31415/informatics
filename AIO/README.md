# AIO Notes

Has some python solutions, harder problems in c++.
Trying to solve all AIO problems rn.

Just a convention, problems from the intermediate set will be named as [px], but if there are different problem 1s etc in the senior set, they would be names [pxsen]. Also, if a problem refers to a number out of bounds, it's probably meaning that the problems are continuous as 1 set from intermediate to senior.